[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "drevo-mcp-bolt"
version = "0.1.0"
description = "Bolt drop-in of the Neo4j knowledge-graph MCP, pointed at drevo"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT OR Apache-2.0" }
authors = [{ name = "drevo contributors", email = "noreply@ice1x.dev" }]
keywords = ["drevo", "neo4j", "bolt", "mcp", "knowledge-graph"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Topic :: Database",
    "Typing :: Typed",
]
# Talks Bolt to a running drevo-server (DREVO_BOLT_PORT) or a real Neo4j —
# the official `neo4j` driver, plus the MCP SDK's FastMCP. No drevo dependency.
dependencies = ["mcp>=1.2", "neo4j>=5,<6"]

[project.optional-dependencies]
dev = [
    "pytest>=7",
    "pytest-asyncio>=0.23",
    "mypy>=1.10",
    "ruff>=0.5",
    "black>=24",
    "faker>=24",
]

[project.scripts]
drevo-mcp-bolt = "drevo_mcp_bolt.server:main"

[project.urls]
Homepage = "https://github.com/ice1x/drevo"
Repository = "https://github.com/ice1x/drevo"

[tool.setuptools]
packages = ["drevo_mcp_bolt"]

[tool.setuptools.package-data]
drevo_mcp_bolt = ["py.typed"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.black]
target-version = ["py310", "py311", "py312", "py313"]
line-length = 100

[tool.mypy]
python_version = "3.10"
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
asyncio_mode = "auto"
