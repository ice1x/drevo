[package]
name = "grapevine"
version = "0.1.0"
edition = "2021"
description = "Embedded graph+vector database combining graph data structures with HNSW ANN search"

[dependencies]
thiserror = "2"

[dev-dependencies]
tempfile = "3"
